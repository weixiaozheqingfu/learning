1.增加jsr303校验，全局异常做判断。

2.增加全局错误的controller处理，并分析与全局异常的不同点。

   应该是全局异常中的异常如果继续抛出，就会被全局错误捕获。或者说有的异常根本就没走全局异常，而是直接被全局错误捕获的，
   比如404.请求还没往“深层次”调用链调用呢，就已经报错了，那么就会被最外层，或者叫最底层的全局错误捕获。

   全局错误，可以作为对全局异常的补充，全局异常该怎么处理就怎么处理。全局错误也是。即可。